interface:
  display_name: "Investment Agent"
  short_description: "Korean and US investment analysis"
  default_prompt: "Use $investment-agent to write today's Korean and US market investment report as a readable Markdown report."

policy:
  allow_implicit_invocation: true
